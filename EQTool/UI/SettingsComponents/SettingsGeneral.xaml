<UserControl x:Class="EQTool.UI.SettingsComponents.SettingsGeneral"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EQTool.UI.SettingsComponents"
             xmlns:vm="clr-namespace:EQTool.ViewModels.SettingsComponents"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:models="clr-namespace:EQTool.Models"
             xmlns:markupExtensions="clr-namespace:EQTool.Services.MarkupExtensions"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="400">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <!-- Solid gold padlock marking built-in triggers/folders in the Triggers tree.
             Drawn as vectors because WPF renders emoji as monochrome outlines only -
             this reproduces the familiar gold-emoji look (gold body, gray shackle). -->
        <DataTemplate x:Key="BuiltInLockIcon">
            <Canvas Width="12" Height="14">
                <Path Stroke="#8A8886" StrokeThickness="1.6" StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                      Data="M 3.2,6.4 V 4.4 A 2.8,2.8 0 0 1 8.8,4.4 V 6.4"/>
                <Rectangle Canvas.Left="1.2" Canvas.Top="6" Width="9.6" Height="7.4" RadiusX="1.8" RadiusY="1.8"
                           Fill="#FFC83D" Stroke="#C88F1A" StrokeThickness="0.8"/>
                <Ellipse Canvas.Left="5" Canvas.Top="8" Width="2" Height="2" Fill="#96690C"/>
                <Rectangle Canvas.Left="5.4" Canvas.Top="9.4" Width="1.2" Height="2.2" RadiusX="0.6" RadiusY="0.6" Fill="#96690C"/>
            </Canvas>
        </DataTemplate>
    </UserControl.Resources>
    <TabControl SelectionChanged="TabControl_SelectionChanged">

        <TabItem Header="General">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Orientation="Vertical" Margin="5, 5, 5, 5">
                <TextBlock Text="Configuration missing!" FontWeight="Bold" FontSize="20" Foreground="Red"
                           Visibility="{Binding MissingConfiguration, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Collapsed}"/>

                <GroupBox Header="Configuration" Margin="0,0,0,6">
                    <StackPanel Orientation="Vertical" Margin="4">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Eq Path"  VerticalAlignment="Center"  FontWeight="Bold"  MinWidth="100" Foreground="Red" Visibility="{Binding DoesNotHaveEqPath, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Collapsed}"/>
                            <TextBlock Text="Eq Path"  VerticalAlignment="Center"  FontWeight="Bold"  MinWidth="100" Foreground="Green" Visibility="{Binding HasEqPath, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Visible}"/>
                            <StackPanel Orientation="Horizontal" >
                                <TextBlock Text="{Binding EqPath}" HorizontalAlignment="Left"/>
                                <Button Margin="5,0,0,0" HorizontalAlignment="Right" Click="EqFolderButtonClicked">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock>Browse</TextBlock>
                                        <Image Source="pack://application:,,,/Images/open-folder.png" Height="{DynamicResource GlobalFontSize}" Margin="5,0,0,0" ></Image>
                                    </StackPanel>
                                </Button>
                            </StackPanel>
                        </StackPanel>
                        <TextBlock TextWrapping="Wrap" Foreground="#FFE0912F" FontWeight="Bold" Margin="0,4,0,0"
                                   Text="{Binding EqPathWarning}"
                                   Visibility="{Binding HasEqPathWarning, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Collapsed}"/>
                        <TextBlock TextWrapping="Wrap" Foreground="#FFE0912F" FontWeight="Bold" Margin="0,4,0,0"
                                   Text="{Binding EqToolPathWarning}"
                                   Visibility="{Binding HasEqToolPathWarning, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Collapsed}"/>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Eq Log Path"  VerticalAlignment="Center"  FontWeight="Bold" MinWidth="100" Foreground="Red" Visibility="{Binding DoesNotHaveEqPath, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Collapsed}"/>
                            <TextBlock Text="Eq Log Path"   VerticalAlignment="Center"  FontWeight="Bold" MinWidth="100" Foreground="Green" Visibility="{Binding HasEqPath, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Visible}"/>
                            <TextBlock Text="{Binding EqLogPath}" HorizontalAlignment="Left" MinWidth="150"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal"  Visibility="{Binding HasEqPath, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Visible}" >
                            <TextBlock Text="Logging"  VerticalAlignment="Center"  FontWeight="Bold"  MinWidth="100" Visibility="{Binding IsLoggingEnabled, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Collapsed}"/>
                            <TextBlock Text="Logging"  VerticalAlignment="Center"  FontWeight="Bold"  MinWidth="100" Foreground="Red"  Visibility="{Binding IsLoggingDisabled, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Visible}"/>
                            <TextBlock Visibility="{Binding IsLoggingEnabled, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Collapsed}" Text="Logging Enabled!" Margin="5,5,0,0" HorizontalAlignment="Left" Foreground="Green"/>
                            <Button Visibility="{Binding IsLoggingDisabled, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Collapsed}" Name="enableLogging" Margin="5,5,0,0" Click="enableLogging_Click">Enable Logging</Button>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Visibility="{Binding IsLoggingDisabled, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Collapsed}"  >
                            <TextBlock HorizontalAlignment="Center" Text="You must enable loogging!"  FontWeight="Bold" Foreground="Red"/>
                        </StackPanel>
                    </StackPanel>
                </GroupBox>

                <StackPanel Orientation="Vertical" HorizontalAlignment="Stretch" Visibility="{Binding NotMissingConfiguration, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Visible}">
                    <TextBlock Visibility="{Binding HasNoCharName}" HorizontalAlignment="Center" Text="Functionality disabled until you log in!"  FontWeight="Bold" Foreground="Red"/>

                    <GroupBox Header="Character" Margin="0,0,0,6">
                        <Grid Margin="4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Character" VerticalAlignment="Center" FontWeight="Bold" MinWidth="80" Margin="0,2,8,2"/>
                            <TextBlock Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="3" Text="{Binding ActivePlayer.Player.Name}" VerticalAlignment="Center" Margin="0,2,0,2"/>

                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Guild" VerticalAlignment="Center" FontWeight="Bold" MinWidth="80" Margin="0,2,8,2"/>
                            <TextBlock Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="3" Text="{Binding ActivePlayer.Player.GuildName}" VerticalAlignment="Center" Margin="0,2,0,2"/>

                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Class" VerticalAlignment="Center" FontWeight="Bold" MinWidth="80" Margin="0,2,8,2"/>
                            <ComboBox Grid.Row="2" Grid.Column="1" IsEnabled="{Binding HasCharName}" SelectedItem="{Binding ActivePlayer.Player.PlayerClass}" ItemsSource="{Binding PlayerClasses}" MinWidth="140" Margin="0,2,12,2"/>
                            <TextBlock Grid.Row="2" Grid.Column="2" Text="Your Level" VerticalAlignment="Center" FontWeight="Bold" MinWidth="80" Margin="0,2,8,2"/>
                            <ComboBox Grid.Row="2" Grid.Column="3" IsEnabled="{Binding HasCharName}" SelectedItem="{Binding ActivePlayer.Player.Level}" ItemsSource="{Binding Levels}" MinWidth="140" HorizontalAlignment="Left" Margin="0,2,0,2"/>

                            <TextBlock Grid.Row="3" Grid.Column="0" Text="Zone" VerticalAlignment="Center" FontWeight="Bold" MinWidth="80" Margin="0,2,8,2"/>
                            <ComboBox Grid.Row="3" Grid.Column="1" IsEnabled="{Binding HasCharName}" SelectedItem="{Binding ActivePlayer.Player.Zone}" ItemsSource="{Binding Zones}" SelectionChanged="zoneselectionchanged" MinWidth="140" Margin="0,2,12,2"/>
                            <TextBlock Grid.Row="3" Grid.Column="2" Text="Track Skill" VerticalAlignment="Center" FontWeight="Bold" MinWidth="80" Margin="0,2,8,2"/>
                            <ComboBox Grid.Row="3" Grid.Column="3" IsEnabled="{Binding ActivePlayer.Player.IsTrackableClass}" SelectedItem="{Binding ActivePlayer.Player.TrackingSkill}" ItemsSource="{Binding TrackSkills}" MinWidth="140" HorizontalAlignment="Left" Margin="0,2,0,2"/>
                        </Grid>
                    </GroupBox>

                    <GroupBox Header="Display" Margin="0,0,0,6">
                        <StackPanel Orientation="Horizontal" Margin="4">
                            <TextBlock Text="Font Size"  FontWeight="Bold"  VerticalAlignment="Center" MinWidth="80" Margin="0,0,8,0"/>
                            <ComboBox SelectedItem="{Binding GlobalFontSize}" ItemsSource="{Binding FontSizes}" SelectionChanged="fontsizescombobox_SelectionChanged" MinWidth="140"/>
                        </StackPanel>
                    </GroupBox>

                    <GroupBox Header="Audio" Margin="0,0,0,6">
                        <StackPanel Orientation="Horizontal" Margin="4">
                            <TextBlock Text="Volume"  FontWeight="Bold"  VerticalAlignment="Center" MinWidth="80" Margin="0,0,8,0"/>
                            <Slider Value="{Binding GlobalAudioVolume}" Minimum="0" Maximum="100" SmallChange="1" LargeChange="10" TickFrequency="10" ValueChanged="SaveSettings" MinWidth="150" VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding GlobalAudioVolume, StringFormat={}{0}%}" VerticalAlignment="Center" MinWidth="35" Margin="8,0,0,0"/>
                            <Button Content="▶" Margin="4,0,0,0" FontSize="7" Width="14" Height="14" Padding="0" Click="testVolume" ToolTip="Test Volume" VerticalAlignment="Center"/>
                        </StackPanel>
                    </GroupBox>

                    <GroupBox Header="UI File Sync" Margin="0,0,0,6">
                        <StackPanel Orientation="Vertical" Margin="4">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Sync UI files" FontWeight="Bold" VerticalAlignment="Center" MinWidth="80" Margin="0,0,8,0"
                                           ToolTip="Automatically back up and sync your character UI files to the server (requires Discord login). Per-character status shows on the Characters tab."/>
                                <CheckBox IsChecked="{Binding SyncUIFiles}" Click="Savesettings" VerticalAlignment="Center"/>
                                <TextBlock Text="(requires Discord login)" Foreground="Gray" VerticalAlignment="Center" Margin="8,0,0,0"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                                <Button Click="SyncUIFilesNow" IsEnabled="{Binding IsDiscordLoggedIn}" MinWidth="100" Margin="0,0,5,0" ToolTip="Two-way sync now: newer files win, missing local files are restored.">Sync Now</Button>
                                <Button Click="RefreshUISyncStatus" IsEnabled="{Binding IsDiscordLoggedIn}" MinWidth="100" ToolTip="Refresh the per-character UI sync status from the server.">Refresh</Button>
                                <Button Click="LogoutDiscord" IsEnabled="{Binding IsDiscordLoggedIn}" MinWidth="120" Margin="5,0,0,0" ToolTip="Clear your saved Discord login on this computer.">Log out of Discord</Button>
                            </StackPanel>
                        </StackPanel>
                    </GroupBox>

                    <GroupBox Header="Overlay Windows" Margin="0,0,0,6">
                        <Grid Margin="4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="1" Text="On top" FontWeight="Bold" HorizontalAlignment="Center" Margin="8,0,8,4"/>
                            <TextBlock Grid.Row="0" Grid.Column="2" Text="Opacity" FontWeight="Bold" Margin="8,0,0,4"/>

                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Damage" VerticalAlignment="Center" FontWeight="Bold" MinWidth="80" Margin="0,2,8,2"/>
                            <CheckBox  Grid.Row="1" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center" IsChecked="{Binding DpsAlwaysOnTop}" Click="SaveAlwaysOntopCheckBoxSettings"/>
                            <Slider    Grid.Row="1" Grid.Column="2" VerticalAlignment="Center" Value="{Binding DPSWindowOpacity}" Minimum=".01" Maximum="1" SmallChange=".1" TickFrequency=".1" ValueChanged="SaveSettings" MinWidth="150" Margin="8,2,0,2"/>

                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Triggers" VerticalAlignment="Center" FontWeight="Bold" MinWidth="80" Margin="0,2,8,2"/>
                            <CheckBox  Grid.Row="2" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center" IsChecked="{Binding SpellAlwaysOnTop}" Click="SaveAlwaysOntopCheckBoxSettings"/>
                            <Slider    Grid.Row="2" Grid.Column="2" VerticalAlignment="Center" Value="{Binding TriggerWindowOpacity}" Minimum=".01" Maximum="1" SmallChange=".1" TickFrequency=".1" ValueChanged="SaveSettings" MinWidth="150" Margin="8,2,0,2"/>

                            <TextBlock Grid.Row="3" Grid.Column="0" Text="Mob" VerticalAlignment="Center" FontWeight="Bold" MinWidth="80" Margin="0,2,8,2"/>
                            <CheckBox  Grid.Row="3" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center" IsChecked="{Binding MobAlwaysOnTop}" Click="SaveAlwaysOntopCheckBoxSettings"/>
                            <TextBlock Grid.Row="3" Grid.Column="2" Text="—" Foreground="Gray" VerticalAlignment="Center" Margin="8,2,0,2"/>

                            <TextBlock Grid.Row="4" Grid.Column="0" Text="Map" VerticalAlignment="Center" FontWeight="Bold" MinWidth="80" Margin="0,2,8,2"/>
                            <CheckBox  Grid.Row="4" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center" IsChecked="{Binding MapAlwaysOnTop}" Click="SaveAlwaysOntopCheckBoxSettings"/>
                            <Slider    Grid.Row="4" Grid.Column="2" VerticalAlignment="Center" Value="{Binding MapWindowOpacity}" Minimum=".01" Maximum="1" SmallChange=".1" TickFrequency=".1" ValueChanged="SaveSettings" MinWidth="150" Margin="8,2,0,2"/>
                        </Grid>
                    </GroupBox>

                    <GroupBox Header="Map" Margin="0,0,0,6">
                        <StackPanel Orientation="Vertical" Margin="4">
                            <StackPanel Orientation="Horizontal" Margin="0,2,0,2">
                                <TextBlock Text="Map Kill timers" ToolTip="Automatically add kill timers to the map. This is a per character setting" VerticalAlignment="Center"  FontWeight="Bold"  MinWidth="120"/>
                                <CheckBox VerticalAlignment="Center" IsChecked="{Binding ActivePlayer.Player.MapKillTimers}"></CheckBox>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,2,0,2">
                                <TextBlock Text="Location Share" VerticalAlignment="Center"  FontWeight="Bold" MinWidth="120"/>
                                <ComboBox SelectedItem="{Binding ActivePlayer.Player.MapLocationSharing}" ItemsSource="{Binding LocationShareOptions}" MinWidth="150"></ComboBox>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,2,0,2">
                                <TextBlock Text="Tracking Radius" ToolTip="Whose tracking radius circles are drawn on the map. This is a per character setting" VerticalAlignment="Center"  FontWeight="Bold" MinWidth="120"/>
                                <ComboBox SelectedItem="{Binding ActivePlayer.Player.MapTrackingVisibility}" ItemsSource="{Binding Source={markupExtensions:EnumBindingSource {x:Type models:TrackingVisibility}}}" MinWidth="150"></ComboBox>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,2,0,2">
                                <TextBlock Text="Map Cache" VerticalAlignment="Center"  FontWeight="Bold"  MinWidth="120"/>
                                <Button Click="deleteMapCache" ToolTip="Do this if you are managing map files and make changes.">Delete Cached Maps</Button>
                            </StackPanel>
                        </StackPanel>
                    </GroupBox>

                    <GroupBox Header="Log Archiving" Margin="0,0,0,6">
                        <StackPanel Orientation="Vertical" Margin="4">
                            <StackPanel Orientation="Horizontal" Margin="0,2,0,2">
                                <TextBlock Text="Enable Archiving" VerticalAlignment="Center" FontWeight="Bold" MinWidth="120"/>
                                <CheckBox VerticalAlignment="Center" IsChecked="{Binding LogArchiveEnabled}" Click="LogArchiveCheckBox_Click"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,2,0,2">
                                <StackPanel.Style>
                                    <Style TargetType="StackPanel">
                                        <Setter Property="Opacity" Value="1.0"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding LogArchiveEnabled}" Value="False">
                                                <Setter Property="Opacity" Value="0.5"/>
                                                <Setter Property="IsEnabled" Value="False"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </StackPanel.Style>
                                <TextBlock Text="Archive at (MB)" VerticalAlignment="Center" FontWeight="Bold" MinWidth="120"/>
                                <TextBox VerticalAlignment="Center" Text="{Binding LogArchiveSizeMB, UpdateSourceTrigger=PropertyChanged}" MinWidth="60" TextChanged="LogArchiveSizeMB_TextChanged"/>
                            </StackPanel>
                            <TextBlock MaxWidth="400" TextWrapping="Wrap" Foreground="Gray" Margin="0,2,0,0"
                                       Text="When a log file exceeds the size above it will be moved to an 'archive' subfolder and renamed with the current date and time."/>
                        </StackPanel>
                    </GroupBox>

                    <GroupBox Header="Login Middlemand" Margin="0,0,0,6">
                        <StackPanel Orientation="Vertical" Margin="4">
                            <StackPanel Orientation="Horizontal" Margin="0,2,0,2">
                                <TextBlock Text="Login middlemand" VerticalAlignment="Center"  FontWeight="Bold" MinWidth="120"/>
                                <CheckBox VerticalAlignment="Center" Name="LoginMiddleMandCheckBox" Click="LoginMiddleMandToggle" MinWidth="75"></CheckBox>
                            </StackPanel>
                            <TextBlock Foreground="Gray" TextWrapping="Wrap" Margin="0,2,0,0"
                                       Text="Login Middlemand is a copy of the code from https://github.com/Zaela/p99-login-middlemand. Please report any issues on discord!"/>
                        </StackPanel>
                    </GroupBox>

                    <GroupBox Header="Triggers" Margin="0,0,0,6">
                        <StackPanel Orientation="Vertical" Margin="4">
                            <StackPanel Orientation="Horizontal" Margin="0,2,0,2">
                                <TextBlock Text="Reset Triggers" VerticalAlignment="Center" FontWeight="Bold" MinWidth="120"/>
                                <Button Click="resetTriggers" MinWidth="100">Reset Triggers</Button>
                            </StackPanel>
                            <TextBlock Foreground="Gray" TextWrapping="Wrap" Margin="0,2,0,0"
                                       Text="Deletes ALL triggers and folders (including any you created) and restores the built-in defaults."/>
                        </StackPanel>
                    </GroupBox>
                </StackPanel>
            </StackPanel>
            </ScrollViewer>
        </TabItem>

        <TabItem Header="Text &amp; Audio Alerts">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Orientation="Vertical" Margin="5, 5, 5, 5">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Installed Voices" VerticalAlignment="Center"  FontWeight="Bold" MinWidth="100"/>
                    <ComboBox SelectedItem="{Binding SelectedVoice}" ItemsSource="{Binding InstalledVoices}" MinWidth="105"></ComboBox>
                    <Button Content="▶" Margin="1" FontSize="7" Width="14" Height="14" Padding="0" Click="textvoice" ToolTip="Test Voice" />
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock VerticalAlignment="Center" FontWeight="Bold"  MinWidth="75"/>
                    <TextBlock Text="Text Alert" VerticalAlignment="Center" FontWeight="Bold"  MinWidth="75"/>
                    <TextBlock Text="Audio Alert" VerticalAlignment="Center" FontWeight="Bold"  />
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Select All Timers"  VerticalAlignment="Center" FontWeight="Bold"  MinWidth="100"/>
                    <Button Content="▶" Margin="1" FontSize="7" Width="14" Height="14" Padding="0" Click="selectallVisual" ToolTip="Select All Visual" />
                    <Button Margin="60,0,0,0" Content="▶" FontSize="7" Width="14" Height="14" Padding="0" Click="selectallAudio" ToolTip="Select All Audio" />
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="FTE" VerticalAlignment="Center" FontWeight="Bold"  MinWidth="100"/>
                    <CheckBox IsChecked="{Binding ActivePlayer.Player.FTEOverlay}" Click="Savesettings" MinWidth="75"></CheckBox>
                    <CheckBox IsChecked="{Binding ActivePlayer.Player.FTEAudio}" Click="Savesettings" MinWidth="30"></CheckBox>
                    <Button Content="▶" Margin="1" FontSize="7" Width="14" Height="14" Padding="0" Click="testFTE" ToolTip="Test FTE" />
                </StackPanel> 
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Root Breaks" VerticalAlignment="Center" FontWeight="Bold"  MinWidth="100"/>
                    <CheckBox IsChecked="{Binding ActivePlayer.Player.RootWarningOverlay}" Click="Savesettings" MinWidth="75"></CheckBox>
                    <CheckBox IsChecked="{Binding ActivePlayer.Player.RootWarningAudio}" Click="Savesettings" MinWidth="30"></CheckBox>
                    <Button Content="▶" Margin="1" FontSize="7" Width="14" Height="14" Padding="0" Click="testRootBreak" ToolTip="Test Root Break" />
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Death Loop" VerticalAlignment="Center" FontWeight="Bold"  MinWidth="100"/>
                    <CheckBox IsChecked="{Binding ActivePlayer.Player.DeathLoopOverlay}" Click="Savesettings" MinWidth="75"></CheckBox>
                    <CheckBox IsChecked="{Binding ActivePlayer.Player.DeathLoopAudio}" Click="Savesettings" MinWidth="30"></CheckBox>
                    <Button Content="▶" Margin="1" FontSize="7" Width="14" Height="14" Padding="0" Click="testDeathLoop" ToolTip="Test Death Loop" />
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Bard Hit Counts" VerticalAlignment="Center" FontWeight="Bold" MinWidth="100"/>
                    <CheckBox IsChecked="{Binding ActivePlayer.Player.BardCountTextAlert}" Click="Savesettings" MinWidth="75"></CheckBox>
                    <CheckBox IsChecked="{Binding ActivePlayer.Player.BardCountAudio}" Click="Savesettings" MinWidth="30"></CheckBox>
                    <Button Content="▶" Margin="1" FontSize="7" Width="14" Height="14" Padding="0" Click="testBardCounts" ToolTip="Test Bard Hit Counts" />
                </StackPanel>
                <Separator/>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Chain Formats" VerticalAlignment="Center" FontWeight="Bold" MinWidth="100"/>
                    <TextBlock>
                        <Hyperlink NavigateUri="https://github.com/smasherprog/EqTool/blob/main/README.md#ch-format-is-loosely-tag-position-ch-target" RequestNavigate="Hyperlink_RequestNavigate">Chain Help</Hyperlink>
                    </TextBlock>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="CH Chain" ToolTip="Complete Heal Overlay" VerticalAlignment="Center"  FontWeight="Bold" MinWidth="100"/>
                    <CheckBox IsChecked="{Binding ActivePlayer.Player.ChChainOverlay}" Click="Savesettings" MinWidth="105"></CheckBox>
                    <Button Content="▶" Margin="1" FontSize="7" Width="14" Height="14" Padding="0" Click="testChChain" ToolTip="Test CH Chain" />
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="CH Chain Tag" ToolTip="Example: GG or CA. Other tags will be ignored" VerticalAlignment="Center" FontWeight="Bold"  MinWidth="100"/>
                    <TextBox Text="{Binding Path=ActivePlayer.Player.ChChainTagOverlay, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" MinWidth="40" TextChanged="CHTextBox_TextChanged"></TextBox>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Ch Warning" ToolTip="Ch Warning" VerticalAlignment="Center" FontWeight="Bold"  MinWidth="100"/>
                    <CheckBox IsChecked="{Binding ActivePlayer.Player.ChChainWarningOverlay}" Click="Savesettings" MinWidth="75"></CheckBox>
                    <CheckBox IsChecked="{Binding ActivePlayer.Player.ChChainWarningAudio}" Click="Savesettings" MinWidth="30"></CheckBox>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Above will warn you when its your turn to cast CH" Foreground="Red"/>
                </StackPanel>
                <Separator/>
                <!-- Trigger window settings (moved here from the former "Old Triggers" tab) -->
                <StackPanel Orientation="Vertical"  Margin="0, 0, 0, 8">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Boat Schedule"  VerticalAlignment="Center"  FontWeight="Bold"  MinWidth="100"/>
                        <CheckBox IsChecked="{Binding ActivePlayer.Player.BoatSchedule}" Click="Savesettings" MinWidth="75"></CheckBox>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Ring 8 Timer"  VerticalAlignment="Center"  FontWeight="Bold"  MinWidth="100"/>
                        <CheckBox IsChecked="{Binding ShowRing8RollTime}" Click="Savesettings" MinWidth="75"></CheckBox>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Scout Timer"  VerticalAlignment="Center"  FontWeight="Bold"  MinWidth="100"/>
                        <CheckBox IsChecked="{Binding ShowScoutRollTime}" Click="Savesettings" MinWidth="75"></CheckBox>
                    </StackPanel> 
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Random Rolls" ToolTip="Show Random rolls in Triggers window" VerticalAlignment="Center"  FontWeight="Bold"  MinWidth="100"/>
                        <CheckBox IsChecked="{Binding ShowRandomRolls}" Click="SaveAlwaysOntopCheckBoxSettings" ></CheckBox>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="If detrimental timer is already running: "
                                   ToolTip="Hint: Select 'StartNewTimer' for targets with same name"
                                   VerticalAlignment="Center"
                                   FontWeight="Bold"
                                   MinWidth="100"/>
                        <ComboBox SelectedItem="{Binding ActivePlayer.Player.TimerRecastSetting}"
                                  ItemsSource="{Binding Source={markupExtensions:EnumBindingSource {x:Type models:TimerRecast}}}"
                                  ToolTip="Hint: Select 'StartNewTimer' for targets with same name"
                                  Margin="0,5,0,0"
                                  FontSize="14"
                                  MinWidth="170"
                                  VerticalAlignment="Center">
                        </ComboBox>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Spells Me only"  VerticalAlignment="Center" FontWeight="Bold"  MinWidth="100"/>
                        <CheckBox IsChecked="{Binding YouOnlySpells}" Click="SaveAlwaysOntopCheckBoxSettings" ></CheckBox>
                    </StackPanel>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Visibility="{Binding ShowClassFilters, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <TextBlock Text="Class Filters" FontWeight="Bold" MinWidth="100" ToolTip="This will filter what shows up on other players."/>
                    <StackPanel Orientation="Vertical">
                        <ListBox IsEnabled="{Binding HasCharName}" ItemsSource="{Binding SelectedPlayerClasses}" MinWidth="150">
                            <ListBox.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Columns="2"/>
                                </ItemsPanelTemplate>
                            </ListBox.ItemsPanel>
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <CheckBox Content="{Binding TheText}" Tag="{Binding TheValue}" Unchecked="CheckBoxZone_Checked" Checked="CheckBoxZone_Checked" IsChecked="{Binding IsChecked}" Margin="0,5,0,0"/>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </StackPanel>
                </StackPanel>
            </StackPanel>
            </ScrollViewer>
        </TabItem>

        <TabItem Header="Triggers">
            <Grid x:Name="TriggersTabRoot" Grid.IsSharedSizeScope="True">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="30*"/>
                    <ColumnDefinition Width="70*"/>
                </Grid.ColumnDefinitions>

                <TreeView Grid.Column="0" SelectedItemChanged="TriggerTree_SelectedItemChanged" PreviewMouseRightButtonDown="TriggerTree_PreviewMouseRightButtonDown" ItemsSource="{Binding TriggerTreeItems}">
                    <TreeView.ItemContainerStyle>
                        <Style TargetType="TreeViewItem">
                            <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}" />
                            <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}" />
                            <EventSetter Event="PreviewMouseRightButtonDown" Handler="OnPreviewMouseRightButtonDown" />
                            <EventSetter Event="PreviewMouseLeftButtonDown" Handler="OnPreviewMouseLeftButtonDown" />
                            <Style.Resources>
                                <!-- Make the native single-selection highlight match the multi-select highlight -->
                                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#FFB3D9FF" />
                                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="Black" />
                                <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" Color="#FFB3D9FF" />
                                <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightTextBrushKey}" Color="Black" />
                            </Style.Resources>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsMultiSelected}" Value="True">
                                    <Setter Property="Background" Value="#FFB3D9FF" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TreeView.ItemContainerStyle>
                    <TreeView.Resources>
                        <HierarchicalDataTemplate DataType="{x:Type vm:TreeGlobal}" ItemsSource="{Binding Children}">
                            <StackPanel Orientation="Horizontal" >
                                <Image Source="pack://application:,,,/Images/open-folder.png" Width="16" Height="16" Margin="0,0,4,0" VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding Name}" Margin="0,0,4,0" VerticalAlignment="Center" />
                            </StackPanel>
                        </HierarchicalDataTemplate>
                        <HierarchicalDataTemplate DataType="{x:Type vm:TreeTriggerFolder}" ItemsSource="{Binding Children}">
                            <StackPanel Orientation="Horizontal">
                                <Image Source="pack://application:,,,/Images/open-folder.png" Width="16" Height="16" Margin="0,0,4,0" VerticalAlignment="Center"/>
                                <!-- Small gold lock marking built-in (read-only) library folders. -->
                                <ContentControl ContentTemplate="{StaticResource BuiltInLockIcon}" Margin="0,0,4,0" VerticalAlignment="Center"
                                                Visibility="{Binding IsBuiltIn, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                <TextBlock Text="{Binding EditableName}" Margin="0,0,4,0" VerticalAlignment="Center">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Style.Triggers>
                                                <!-- Built-in library folders differ from user folders only by
                                                     their gold lock icon; the text styling is identical. -->
                                                <DataTrigger Binding="{Binding IsBuiltIn}" Value="True">
                                                    <Setter Property="ToolTip" Value="Built-in folder (shipped with EQTool)"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding IsEditing}" Value="True">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                <TextBox Text="{Binding EditableName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                         MinWidth="90" Margin="0,0,4,0" Padding="1,0,1,0"
                                         KeyDown="RenameTextBox_KeyDown"
                                         LostFocus="RenameTextBox_LostFocus"
                                         IsVisibleChanged="RenameTextBox_IsVisibleChanged">
                                    <TextBox.Style>
                                        <Style TargetType="TextBox">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsEditing}" Value="True">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBox.Style>
                                </TextBox>
                            </StackPanel>
                        </HierarchicalDataTemplate>
                        <HierarchicalDataTemplate DataType="{x:Type vm:TreeTrigger}" ItemsSource="{Binding Children}">
                            <!-- Built-in triggers are visually distinct from user triggers solely by
                                 their icon: a gold lock instead of the lightning bolt. Text styling is
                                 identical so enabled/disabled coloring stays the only text cue. -->
                            <StackPanel Orientation="Horizontal">
                                <StackPanel.Style>
                                    <Style TargetType="StackPanel">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsBuiltIn}" Value="True">
                                                <Setter Property="ToolTip" Value="Built-in trigger (shipped with EQTool). Its name, search text and zone are locked; outputs and timers can still be edited."/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </StackPanel.Style>
                                <!-- User triggers keep the lightning bolt; built-ins swap it for the gold lock. -->
                                <TextBlock Text="🗲" Margin="0,0,2,0" VerticalAlignment="Center">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsBuiltIn}" Value="True">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                <ContentControl ContentTemplate="{StaticResource BuiltInLockIcon}" Margin="0,0,4,0" VerticalAlignment="Center"
                                                Visibility="{Binding IsBuiltIn, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                <TextBlock Text="{Binding Name}">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Style.Triggers>
                                                <!-- Gray out any disabled trigger (built-in or user). -->
                                                <DataTrigger Binding="{Binding IsTriggerEnabled}" Value="False">
                                                    <Setter Property="Foreground" Value="Gray"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </StackPanel>
                        </HierarchicalDataTemplate>
                    </TreeView.Resources>
                </TreeView>
                <ContentControl Grid.Column="1" Content="{Binding TriggerUserControl}"/>
            </Grid>
        </TabItem>

        <TabItem Header="Characters">
            <Grid x:Name="CharactersTabRoot" Grid.IsSharedSizeScope="True">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="30*"/>
                    <ColumnDefinition Width="70*"/>
                </Grid.ColumnDefinitions>

                <TreeView Grid.Column="0" SelectedItemChanged="CharacterTree_SelectedItemChanged" ItemsSource="{Binding CharacterTreeItems}">
                    <TreeView.ItemContainerStyle>
                        <Style TargetType="TreeViewItem">
                            <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}" />
                            <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}" />
                            <EventSetter Event="PreviewMouseRightButtonDown" Handler="OnPreviewMouseRightButtonDown" />
                            <EventSetter Event="PreviewMouseLeftButtonDown" Handler="CharacterTree_PreviewMouseLeftButtonDown" />
                            <Style.Resources>
                                <!-- Make the native single-selection highlight match the multi-select highlight -->
                                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#FFB3D9FF" />
                                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="Black" />
                                <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" Color="#FFB3D9FF" />
                                <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightTextBrushKey}" Color="Black" />
                            </Style.Resources>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsMultiSelected}" Value="True">
                                    <Setter Property="Background" Value="#FFB3D9FF" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TreeView.ItemContainerStyle>
                    <TreeView.Resources>
                        <HierarchicalDataTemplate DataType="{x:Type vm:TreeServer}" ItemsSource="{Binding Children}">
                            <StackPanel Orientation="Horizontal" >
                                <TextBlock Text="🖧" />
                                <TextBlock Text="{Binding Name}" Margin="0,0,4,0" />
                            </StackPanel>
                        </HierarchicalDataTemplate>
                        <HierarchicalDataTemplate DataType="{x:Type vm:TreeZone}" ItemsSource="{Binding Children}">
                            <StackPanel Orientation="Horizontal" >
                                <TextBlock Text="🖧" />
                                <TextBlock Text="{Binding Name}" Margin="0,0,4,0" />
                            </StackPanel>
                        </HierarchicalDataTemplate>
                        <HierarchicalDataTemplate DataType="{x:Type vm:TreePlayer}" ItemsSource="{Binding Children}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition SharedSizeGroup="PlayerName"/>
                                    <ColumnDefinition SharedSizeGroup="PlayerSync"/>
                                    <ColumnDefinition SharedSizeGroup="lastPlayedDate"/>
                                    <ColumnDefinition SharedSizeGroup="SyncDate"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="🐉"/>
                                <TextBlock Grid.Column="0" Text="{Binding Name}" Margin="16,0,0,0" />
                                <TextBlock Grid.Column="1" FontWeight="Bold" Margin="4,0,0,0" VerticalAlignment="Center" ToolTip="UI file sync status">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value=""/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding UiSyncStatus}" Value="Synced">
                                                    <Setter Property="Text" Value="✔"/>
                                                    <Setter Property="Foreground" Value="Green"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding UiSyncStatus}" Value="NotSynced">
                                                    <Setter Property="Text" Value="⊗"/>
                                                    <Setter Property="Foreground" Value="Red"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                <TextBlock Grid.Column="2" Text="{Binding LastPlayed}" Margin="8,0,0,0" />
                                <TextBlock Grid.Column="3" Text="{Binding UiSyncDate}" Margin="8,0,0,0" Foreground="Gray" ToolTip="Last UI sync (UTC)" />
                            </Grid>
                        </HierarchicalDataTemplate>
                    </TreeView.Resources>
                </TreeView>
                <ContentControl Grid.Column="1" Content="{Binding CharacterUserControl}"/>
            </Grid>
        </TabItem>

        <TabItem Header="Experimental">
            <StackPanel Orientation="Vertical" Margin="5, 5, 5, 5" HorizontalAlignment="Stretch">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Timer Sharing"  VerticalAlignment="Center"  FontWeight="Bold"  MinWidth="100"/>
                    <CheckBox IsChecked="{Binding ActivePlayer.Player.ShareTimers}" Click="Savesettings" MinWidth="75"></CheckBox>
                </StackPanel>
                <DockPanel>
                    <TextBlock MaxWidth="400" TextWrapping="Wrap" Text="When this is checked, timers will be shared with those around you for detrimental spells. So, if you slow a mob, anyone within range will also see the slow timer. Same for dragon effects!" />
                </DockPanel>
                <TextBlock Text="Console Debugging" HorizontalAlignment="Center" FontWeight="Bold" Margin="5,5,0,0"/>
                <StackPanel Orientation="Vertical" Margin="0, 0, 0, 8">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Window" VerticalAlignment="Center" FontWeight="Bold" MinWidth="100"/>
                        <Button Click="openConsoleWindow">Open</Button>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Map Logging" VerticalAlignment="Center"  FontWeight="Bold"  MinWidth="100"/>
                        <CheckBox x:Name="MapConsoleLog" Click="toggleMapConsoleOutput" MinWidth="75"></CheckBox>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Spell Logging" VerticalAlignment="Center"  FontWeight="Bold"  MinWidth="100"/>
                        <CheckBox Name="SpellConsoleLog" Click="toggleSpellConsoleOutput" MinWidth="75"></CheckBox>
                    </StackPanel>
                </StackPanel>
            </StackPanel>
        </TabItem>

        <TabItem Header="UI">
            <ScrollViewer>
                <StackPanel Orientation="Vertical" Margin="5, 5, 5, 5" HorizontalAlignment="Stretch">
                    <StackPanel Orientation="Vertical">
                        <TextBlock Text="You cannot have EQ runing for these operations to be successful." FontWeight="Bold" />
                        <TextBlock  TextWrapping="Wrap" Text="Select a class and the master template. This will override all other UI files with the samel class in your EQ folder." />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <ComboBox Name="UIClassMaster" IsEditable="True" IsReadOnly="True" Text="-- Select Class --"  ItemsSource="{Binding PlayerClasses}" MinWidth="100"></ComboBox>
                        <ComboBox Name="SelectedUIFile"  IsEditable="True" IsReadOnly="True" Text="-- Select UI File --" ItemsSource="{Binding UIFiles}" MinWidth="100" Margin="5, 0, 0, 0" ></ComboBox>
                        <Button Click="SelectMasterUIFile" MinWidth="100" Margin="5, 0, 0, 0" >Use as Master</Button>
                        <Button Content="⟳" Margin="5, 0, 0, 0" FontSize="7" Width="14"  Click="RefreshUIFiles" ToolTip="Refresh UI list" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Night Vision Fix" Margin="0, 3, 0, 0" FontWeight="Bold" />
                        <Button Click="FixNightVision" Margin="5, 0, 0, 0">Fix Night vision</Button>
                    </StackPanel>
                    <StackPanel Orientation="Vertical">
                        <TextBlock HorizontalAlignment="Center" Text="If you want to revert, download the patch files from project1999 and apply them."></TextBlock>
                        <TextBlock HorizontalAlignment="Center" Text="Click images below to see the larger images!"></TextBlock>
                        <Button Click="BadNightVision" MaxWidth="500">
                            <Image Source="https://pigparse.azurewebsites.net/Content/bad.png"/>
                        </Button>
                        <Button Click="GoodNightVision" MaxWidth="500">
                            <Image Source="https://pigparse.azurewebsites.net/Content/good.png"/>
                        </Button>
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
        </TabItem>

        <TabItem Header="Friends">
            <StackPanel Orientation="Vertical" Margin="5,5,5,5">
                <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                    <TextBlock Text="Server" FontWeight="Bold" VerticalAlignment="Center" MinWidth="80"/>
                    <ComboBox x:Name="FriendsServerComboBox" MinWidth="150" SelectionChanged="FriendsServerComboBox_SelectionChanged"/>
                </StackPanel>
                <TextBlock Text="Friends (one per line):" FontWeight="Bold" Margin="0,0,0,2"/>
                <TextBox x:Name="FriendsTextBox" Height="300" AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto" TextWrapping="NoWrap"
                         FontFamily="Courier New" FontSize="13" Padding="3"/>
                <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                    <Button Click="LoadFriendsButton_Click" MinWidth="100" Margin="0,0,5,0">Reload</Button>
                    <Button Click="PushFriendsButton_Click" MinWidth="180">Push to All Character Files</Button>
                </StackPanel>
                <TextBlock TextWrapping="Wrap" Foreground="Gray" Margin="0,5,0,0"
                           Text="Aggregates the [Friends] section from all character files for the selected server. Push overwrites that section in every file."/>
            </StackPanel>
        </TabItem>

        <TabItem Header="Debug" Name="DebugTab">
            <StackPanel Orientation="Vertical" Margin="5, 5, 5, 5" HorizontalAlignment="Stretch">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Random Roll"  VerticalAlignment="Center"  FontWeight="Bold" MinWidth="100"/>
                    <Button Click="testRandomRolls" MinWidth="100">Test Random Rolls</Button>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="DPS"  VerticalAlignment="Center"  FontWeight="Bold" MinWidth="100"/>
                    <Button Click="testDPS" MinWidth="100">Add Test DPS</Button>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Spells"  VerticalAlignment="Center"  FontWeight="Bold" MinWidth="100"/>
                    <Button Click="testspellsclicked"  MinWidth="100">Add Test Spells</Button>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Share Timers"  VerticalAlignment="Center"  FontWeight="Bold" MinWidth="100"/>
                    <Button Click="testsharetimers"  MinWidth="100">Share Timers Test</Button>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Map"  VerticalAlignment="Center"  FontWeight="Bold" MinWidth="100"/>
                    <Button Click="textmapclicked" MinWidth="100">Test Locations</Button>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Map"  VerticalAlignment="Center"  FontWeight="Bold" MinWidth="100"/>
                    <Button Click="testsignalrlocations" MinWidth="100">Test Signalr Locations</Button>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Run Log File"  VerticalAlignment="Center"  FontWeight="Bold" MinWidth="100"/>
                    <Button Click="selectLogFile">Select File</Button>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                    <TextBlock Text="Log Push"  VerticalAlignment="Center"  FontWeight="Bold" MinWidth="100"/>
                    <TextBox x:Name="LogPushText" MinWidth="150"></TextBox>
                    <Button Click="logpush">Push</Button>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Discord API"  VerticalAlignment="Center"  FontWeight="Bold" MinWidth="100"/>
                    <Button Click="testSecuredApi_Click" MinWidth="100">Test Secured API</Button>
                </StackPanel>
            </StackPanel>
        </TabItem>
    </TabControl>
</UserControl>
